## Day 1: Hardware Setup & Repository Initialization

**Objective:** Prepare the physical USB drive and set up the version control safely.

### 🛠️ Tasks Completed Today:
- [x] Repository Setup: Initialized the GitHub repository and established the project roadmap in the README.md.
- [x] Safety First (.gitignore): Created a strict .gitignore file. Why? Because portable Python environments (/env/) and local AI models (.gguf) are massive (several gigabytes). If Git tries to track them, the repository will crash. Only the core logic and scripts will be tracked here.
- [x] Hardware Prep: Formatted a high-speed USB 3.2 drive to NTFS / exFAT. This is mandatory because FAT32 cannot hold individual files larger than 4GB, and our AI model will be around 4.5GB.
- [x] Speed Test: Verified the USB drive read speeds (aiming for ~300 MB/s). High read speeds are critical so the AI "Brain" loads into the computer's RAM quickly when plugged in.

## Day 2: The Portable Environment (/env)

**Objective:** Create a 100% isolated and standalone Python environment directly on the USB drive so the AI doesn't rely on the host computer.

### 🛠️ Tasks Completed Today:
- [x] **Downloaded WinPython:** Acquired a standalone version of Python (WinPython 3.10.x "Dot" version) which is designed to run directly from a portable drive without installation.
- [x] **Extracted to USB:** Carefully extracted the entire WinPython package directly into the root of the USB drive, placing it inside a folder named `/env`.
- [x] **Located the Executable:** Navigated through the new `/env` directory to find the exact path to `python.exe`. This is a critical step because our future launch script needs to know exactly where the "Brain's Engine" is located. *(Typical path: `env\WPy64-31090\python-3.10.9.amd64\python.exe`)*
- [x] **Verified Safety Protocol:** Confirmed that the `.gitignore` file created on Day 1 is successfully blocking this massive 1GB+ `/env` folder from uploading to GitHub, keeping the repository fast and clean.

## Day 3: The Ignition Spark (launch.bat)

**Objective:** Create a dynamic batch script that automatically finds the USB drive letter and launches the portable Python environment, completely bypassing the host computer's system.

### 🛠️ Tasks Completed Today:
- [x] **Created `launch.bat`:** Wrote the batch script that acts as the "ignition key" for the Neural Key.
- [x] **Dynamic Pathing (`%~dp0`):** Used Windows batch variables to automatically detect the current USB drive letter (e.g., `D:\` or `E:\`) so the AI works on any computer without hardcoding paths.
- [x] **Targeted Portable Python:** Configured the script to explicitly point to the `python.exe` inside our `/env` folder instead of relying on the host PC.
- [x] **Script Execution Setup:** Set the script to launch `jarvis.py` (which we will build on Day 4) once the environment is loaded.

## Day 4: The "Hello World" Core (jarvis.py)

**Objective:** Create a baseline Python script to prove that the `launch.bat` file successfully triggers the isolated Python environment from the USB drive.

### 🛠️ Tasks Completed Today:
- [x] **Created `jarvis.py`:** Wrote the initial core script for the AI assistant.
- [x] **System Path Verification:** Added diagnostic code (`sys.executable` and `os.getcwd()`) to print the exact location of the Python engine being used. 
- [x] **Proof of Isolation:** This script physically proves the code is running entirely from the USB drive's `/env` folder, keeping the host PC untouched.

## Day 5: Relative Path Mastery

**Objective:** Ensure the AI can locate its future brain (the models) regardless of what drive letter (D:, E:, F:, etc.) the host PC assigns to the USB stick.

### 🛠️ Tasks Completed Today:
- [x] **Refactored `jarvis.py`:** Added dynamic absolute path resolution.
- [x] **Path Calculation:** Used Python's `os.path.abspath(__file__)` to programmatically determine the USB root folder at runtime.
- [x] **Auto-Folder Generation:** Added logic to automatically detect and create the `/models` directory if it doesn't exist, preparing the exact structure needed for Week 2.

## Day 6: Verification & Testing

**Objective:** Physically test the USB drive on a completely different computer to guarantee the portable environment works 100% offline without any pre-installed software.

### 🛠️ Tasks Completed Today:
- [x] **Cross-Device Testing:** Unplugged the USB drive and plugged it into a different Windows machine.
- [x] **Execution Success:** Double-clicked `launch.bat` on the new machine.
- [x] **Verification Passed:** The script successfully dynamically identified the new drive letter, launched the isolated Python environment, and verified the `/models` directory path without throwing any system errors. Week 1 Foundation is solid!

## Day 7: Rest & Prep for Week 2

**Objective:** Review Week 1 accomplishments, take a well-deserved break, and prep the repository for the heavy lifting of Week 2 (Intelligence & Inference).

### 🛠️ Tasks Completed Today:
- [x] **Project Review:** Confirmed all foundational scripts (`launch.bat`, `jarvis.py`) and relative pathing work perfectly across different machines.
- [x] **README Update:** Updated the main repository page with the Week 2 roadmap to track upcoming goals.
- [x] **Rest:** Took a break to prepare for downloading massive files and configuring the AI brain tomorrow.

## Day 8: Installing the AI Engine

**Objective:** Install the required AI (LLM) and Text-to-Speech libraries strictly inside the portable USB environment.

### 🛠️ Tasks Completed Today:
- [x] Booted portable console using `WinPython Command Prompt.exe`.
- [x] Installed `llama-cpp-python` (CPU version) for offline AI thinking.
- [x] Installed `pyttsx3` (Offline Text-to-Speech) for the AI's voice.  

## Day 9: Acquiring the Brain

**Objective:** Download the offline Large Language Model (LLM) directly into the USB drive.

### 🛠️ Tasks Completed Today:
- [x] Downloaded a lightweight, 4-bit quantized `.gguf` model for CPU performance.
- [x] Placed the model inside the `/models` directory on the USB.
- [x] Renamed the model file to `brain.gguf`.

## Day 10: The Brain Connection

**Objective:** Write the initial inference script to successfully load the model into RAM and generate a response.

### 🛠️ Tasks Completed Today:
- [x] Integrated `llama_cpp` into `jarvis.py`.
- [x] Set `n_gpu_layers=0` to guarantee the code won't crash on computers without dedicated graphics cards.
- [x] Successfully passed a test prompt and received an offline generated response.

## Day 11: The Interactive Chat Loop

**Objective:** Upgrade the AI from a single-prompt script into a continuous interactive terminal chat.

### 🛠️ Tasks Completed Today:
- [x] Added a `while True` loop to keep the AI awake for continuous conversation.
- [x] Integrated Python's `input()` function to take live text queries.
- [x] Added graceful exit commands (`exit`, `quit`) to safely shut down the LLM.

## Day 12: Adding the Voice (TTS)

**Objective:** Give the AI a voice using the offline `pyttsx3` library.

### 🛠️ Tasks Completed Today:
- [x] Initialized the text-to-speech engine.
- [x] Created a custom `speak(text)` function to handle audio output seamlessly.
- [x] The AI now verbally announces when it boots up and speaks all of its generated responses completely offline.

## Day 13: Full Offline System Test

**Objective:** Verify that the entire LLM + TTS stack functions with zero internet access.

### 🛠️ Tasks Completed Today:
- [x] Disabled all network adapters on the host PC.
- [x] Ran `launch.bat`.
- [x] **Success:** The model loaded from the USB into RAM, accepted a text prompt, generated a text response, and synthesized audio playback seamlessly. The "Neural Key" is fully functional offline.

## Day 14: Week 2 Check-off & Review

**Objective:** Finalize Week 2 documentation and prepare for Week 3.

### 🛠️ Tasks Completed Today:
- [x] Marked all Intelligence & Inference goals as completed on the main project page.
- [x] Code Review: Ensured `jarvis.py` is clean and thoroughly commented.
- [x] Taking a rest day before Week 3 (Automation & GUI).

## Day 15: Installing the "Ears" (Vosk Setup)

**Objective:** Set up the offline speech recognition engine to give JARVIS-on-a-Stick the ability to listen securely without transmitting audio to the cloud.

### 🛠️ Tasks Completed Today:
- [x] Booted the portable `/env` terminal using WinPython Command Prompt.
- [x] Installed the `vosk` and `sounddevice` Python libraries for offline audio processing.
- [x] Downloaded the lightweight Vosk English model (`vosk-model-small-en-us`).
- [x] Extracted and placed the model into the `/models` directory alongside the LLM brain, renaming it to `vosk_model`.
- [x] Verified that `.gitignore` prevents these heavy audio models from tracking.

## Day 16: The Listening Script (Speech-to-Text)

**Objective:** Create a standalone Python script to test the microphone and verify that Vosk can translate live speech to text entirely offline.

### 🛠️ Tasks Completed Today:
- [x] Created `listen_test.py`.
- [x] Integrated `sounddevice` to capture raw audio streams directly from the host PC's default microphone.
- [x] Used `vosk.KaldiRecognizer` to decode the audio queue into JSON format and extract the pure text string.
- [x] Successfully calibrated the microphone and transcribed live, offline speech.

